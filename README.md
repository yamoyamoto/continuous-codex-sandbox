# continuous-codex-sandbox

End-to-end test sandbox for [`yamoyamoto/continuous-codex`](https://github.com/yamoyamoto/continuous-codex).

This repository exists solely to exercise `continuous-codex` against a real `codex exec` invocation. Iterations create branches, push commits, open PRs, wait for CI, and merge.

## Initial state

Empty repository with just this README.
